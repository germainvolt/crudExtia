server:
    address: localhost
    port: 8080
logging:
    level:
        org.springframework.web: DEBUG

spring:
    h2:
        console:
            enabled: true
    datasource:
        name: crudextia
        url: jdbc:h2:./target/crud-extia